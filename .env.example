# CampSolver public website — copy to .env and fill in real values
# Base URL of the public-read REST API (no trailing slash)
VITE_API_BASE_URL=https://api.example.com
# Base URL of the Socket.IO server (public namespace is appended automatically)
VITE_SOCKET_URL=https://api.example.com
